namespace Timesheet.Shared.Utils
{
    public enum ApprovalType
    {
        Period = 1,
        Timeoff = 2,
    }

    public enum ApprovalStatusOption
    {
        NotSubmited = 0,
        Waiting = 1,
        Approved = 2,
        Rejected = 3,
        AdminApproval = 4,
    }
}
